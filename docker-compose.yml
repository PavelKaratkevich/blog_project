version: '3.9'

services:
    blog_db:
      image: 'postgres:latest'
      restart: always
      ports:
        - 5432:5432
      env_file:
        - .env  
      volumes:
        - ./db-data/:/var/lib/postgresql/data/
    
volumes:
  db:
    driver: local